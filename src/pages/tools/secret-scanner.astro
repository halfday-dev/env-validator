---
import Layout from '../../layouts/Layout.astro';
import SecretScanner from '../../components/SecretScanner.jsx';
const title = "Secret Scanner — Find Leaked API Keys & Credentials Instantly | Halfday";
const description = "Paste any text to instantly detect leaked API keys, passwords, and tokens. Runs entirely in your browser — your data never leaves the page.";
---
<Layout title={title} description={description}>
  <SecretScanner client:load />
</Layout>
