{
  "name": "halfday-env-scan",
  "type": "module",
  "version": "1.0.0",
  "description": "Scan .env files for leaked API keys, weak passwords, and security issues",
  "keywords": ["env", "dotenv", "security", "scanner", "secrets", "api-keys", "cli"],
  "bin": {
    "halfday-env-scan": "./src/cli.js"
  },
  "files": ["src/lib/scanner.js", "src/cli.js"],
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "@tailwindcss/typography": "^0.5.19",
    "astro": "^4.16.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
